<h1> Register Publishers </h1>

<%=form_for @publisher do |f| %>

	<% if @publisher.errors.any? %>
  <div id="error">
    <h2>
      <%= pluralize(@publisher.errors.count, "error") %> prohibited
      this publisher from being saved:
    </h2>
    <ul>
      <% @publisher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

	<p>
		<%= f.label :Name %>
		<%= f.text_field :name %>
	</p>

	<p>
		<%= f.submit :Submit %>
		
	</p>
<% end %>