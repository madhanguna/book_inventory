<!DOCTYPE html>
<html>
<head>
  <title>AppBook</title>
 </head>
<body>
	<div id="container">
	<div id="user_header">
	<% if current_user %>
		Welcome <%= current_user.email %>.
		<%= link_to "Log Out",logout_path %>
		<center><%= link_to "Book Management",books_index_path %></center><br/>
		<center><%= link_to "Ecommerce",root_url %></center>
	<% else %>
		<%= link_to "Sign Up",signup_path %> or
		<%= link_to "Log In",login_path %>

		<% end %>
	</div>

	<% flash.each do |name,msg| %>
	<%= content_tag :div ,msg,id: "flash_#{name}" %>
	<% end %>

<%= yield %>
</div>

</body>
</html>
